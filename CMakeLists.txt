# SPDX-License-Identifier: LGPL-3.0-or-later
cmake_minimum_required(VERSION 3.13)
project(visiong LANGUAGES C CXX)

set(CMAKE_C_STANDARD 99)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)

option(VISIONG_BUILD_PYTHON_MODULE "Build the Python extension module (_visiong.so)." ON)
option(VISIONG_BUILD_CPP_LIBS "Build libvisiong shared/static libraries." ON)
option(VISIONG_ENABLE_LTO "Enable interprocedural optimization when supported." ON)
option(VISIONG_STRIP_SYMBOLS "Strip symbols from shared libraries at link time." ON)
option(VISIONG_PERF_FIRST "Prefer runtime performance over binary size." ON)
option(VISIONG_ENABLE_FAST_MATH "Enable -ffast-math (may reduce numerical stability)." ON)
option(VISIONG_ENABLE_IVE "Build the IVE component." ON)
option(VISIONG_ENABLE_NPU "Build the NPU component." ON)
option(VISIONG_ENABLE_GUI "Build the GUI component." ON)

set(VISIONG_VERSION "1.0.0" CACHE STRING "Public version string for the Python module and C++ library.")
set(VISIONG_ROOT "${CMAKE_CURRENT_SOURCE_DIR}")

set(VISIONG_SDK_ROOT "" CACHE PATH "Root directory of a Luckfox/Rockchip SDK.")
set(VISIONG_DEPS_ROOT "" CACHE PATH "Root directory of a staged dependency bundle generated by scripts/stage_release_deps.sh.")
set(VISIONG_VENDOR_LIB_DIR "" CACHE PATH "Directory containing vendor prebuilt libraries.")
set(VISIONG_IVE_LIB_DIR "" CACHE PATH "Directory containing prebuilt IVE libraries.")
set(VISIONG_PYTHON_ROOT "" CACHE PATH "Directory containing the target Python runtime sysroot.")
set(VISIONG_OPENCV_DIR "" CACHE PATH "Path to OpenCVConfig.cmake or its containing directory.")
set(VISIONG_OPENCV_ROOT "" CACHE PATH "Root of a staged official OpenCV install tree.")
set(VISIONG_PYTHON_INCLUDE_DIR "" CACHE PATH "Path to the target Python include directory.")
set(VISIONG_PYTHON_LIBRARY "" CACHE FILEPATH "Path to the target Python shared library.")
set(VISIONG_VENDOR_INCLUDE_DIR "" CACHE PATH "Directory containing Rockchip SDK headers (rk_*, rkaiq, sample_comm).")
set(VISIONG_RGA_INCLUDE_DIR "" CACHE PATH "Directory containing librga headers (im2d.h).")
set(VISIONG_IVE_INCLUDE_DIR "" CACHE PATH "Directory containing IVE headers (rk_mpi_ive.h).")
set(VISIONG_RKNN_INCLUDE_DIR "" CACHE PATH "Directory containing RKNN runtime headers (rknn_api.h).")

function(visiong_first_existing out_var)
    foreach(candidate IN LISTS ARGN)
        if(candidate AND EXISTS "${candidate}")
            set(${out_var} "${candidate}" PARENT_SCOPE)
            return()
        endif()
    endforeach()
    set(${out_var} "" PARENT_SCOPE)
endfunction()

function(visiong_require_path path_value hint)
    if(NOT EXISTS "${path_value}")
        message(FATAL_ERROR "${hint}
Missing path: ${path_value}")
    endif()
endfunction()

function(visiong_apply_common_compile_settings target_name)
    target_compile_options(${target_name} PRIVATE
        $<$<COMPILE_LANGUAGE:C>:${VISIONG_WARN_FLAGS}>
        $<$<COMPILE_LANGUAGE:CXX>:${VISIONG_WARN_FLAGS}>
    )
    target_compile_definitions(${target_name} PRIVATE ${VISIONG_COMMON_COMPILE_DEFINITIONS})
endfunction()

if(VISIONG_DEPS_ROOT)
    if(VISIONG_VENDOR_LIB_DIR STREQUAL "")
        visiong_first_existing(_vendor_from_deps
            "${VISIONG_DEPS_ROOT}/lib"
            "${VISIONG_DEPS_ROOT}/stage/lib"
        )
        if(_vendor_from_deps)
            set(VISIONG_VENDOR_LIB_DIR "${_vendor_from_deps}" CACHE PATH "Directory containing vendor prebuilt libraries." FORCE)
        endif()
    endif()

    if(VISIONG_IVE_LIB_DIR STREQUAL "")
        visiong_first_existing(_ive_from_deps
            "${VISIONG_DEPS_ROOT}/3rdparty/ive/ive/lib"
            "${VISIONG_DEPS_ROOT}/ive/ive/lib"
            "${VISIONG_DEPS_ROOT}/ive/lib"
        )
        if(_ive_from_deps)
            set(VISIONG_IVE_LIB_DIR "${_ive_from_deps}" CACHE PATH "Directory containing prebuilt IVE libraries." FORCE)
        endif()
    endif()

    if(VISIONG_PYTHON_ROOT STREQUAL "")
        visiong_first_existing(_python_root_from_deps
            "${VISIONG_DEPS_ROOT}/3rdparty/target_python"
            "${VISIONG_DEPS_ROOT}/target_python"
            "${VISIONG_DEPS_ROOT}/python"
        )
        if(_python_root_from_deps)
            set(VISIONG_PYTHON_ROOT "${_python_root_from_deps}" CACHE PATH "Directory containing the target Python runtime sysroot." FORCE)
        endif()
    endif()

    if(VISIONG_OPENCV_ROOT STREQUAL "")
        visiong_first_existing(_opencv_root_from_deps
            "${VISIONG_DEPS_ROOT}/opencv"
            "${VISIONG_DEPS_ROOT}/opencv/install"
        )
        if(_opencv_root_from_deps)
            set(VISIONG_OPENCV_ROOT "${_opencv_root_from_deps}" CACHE PATH "Root of a staged official OpenCV install tree." FORCE)
        endif()
    endif()

    if(VISIONG_VENDOR_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_vendor_include_from_deps
            "${VISIONG_DEPS_ROOT}/vendor/rockchip/include"
            "${VISIONG_DEPS_ROOT}/include/rockchip_sdk"
            "${VISIONG_DEPS_ROOT}/include"
        )
        if(_vendor_include_from_deps)
            set(VISIONG_VENDOR_INCLUDE_DIR "${_vendor_include_from_deps}" CACHE PATH "Directory containing Rockchip SDK headers (rk_*, rkaiq, sample_comm)." FORCE)
        endif()
    endif()

    if(VISIONG_RGA_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_rga_include_from_deps
            "${VISIONG_DEPS_ROOT}/vendor/librga/include"
            "${VISIONG_DEPS_ROOT}/librga/include"
            "${VISIONG_DEPS_ROOT}/include/librga"
        )
        if(_rga_include_from_deps)
            set(VISIONG_RGA_INCLUDE_DIR "${_rga_include_from_deps}" CACHE PATH "Directory containing librga headers (im2d.h)." FORCE)
        endif()
    endif()

    if(VISIONG_IVE_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_ive_include_from_deps
            "${VISIONG_DEPS_ROOT}/vendor/ive/include"
            "${VISIONG_DEPS_ROOT}/ive/include"
            "${VISIONG_DEPS_ROOT}/include/ive"
        )
        if(_ive_include_from_deps)
            set(VISIONG_IVE_INCLUDE_DIR "${_ive_include_from_deps}" CACHE PATH "Directory containing IVE headers (rk_mpi_ive.h)." FORCE)
        endif()
    endif()

    if(VISIONG_RKNN_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_rknn_include_from_deps
            "${VISIONG_DEPS_ROOT}/vendor/rknpu2/include"
            "${VISIONG_DEPS_ROOT}/rknpu2/include"
            "${VISIONG_DEPS_ROOT}/include/rknpu2"
        )
        if(_rknn_include_from_deps)
            set(VISIONG_RKNN_INCLUDE_DIR "${_rknn_include_from_deps}" CACHE PATH "Directory containing RKNN runtime headers (rknn_api.h)." FORCE)
        endif()
    endif()
endif()

if(VISIONG_SDK_ROOT)
    if(VISIONG_VENDOR_LIB_DIR STREQUAL "")
        visiong_first_existing(_vendor_from_sdk
            "${VISIONG_SDK_ROOT}/lib"
            "${VISIONG_SDK_ROOT}/stage/lib"
            "${VISIONG_SDK_ROOT}/build/lib"
            "${VISIONG_SDK_ROOT}/runtime/lib"
        )
        if(_vendor_from_sdk)
            set(VISIONG_VENDOR_LIB_DIR "${_vendor_from_sdk}" CACHE PATH "Directory containing vendor prebuilt libraries." FORCE)
        endif()
    endif()

    if(VISIONG_IVE_LIB_DIR STREQUAL "")
        visiong_first_existing(_ive_from_sdk
            "${VISIONG_SDK_ROOT}/3rdparty/ive/ive/lib"
            "${VISIONG_SDK_ROOT}/ive/ive/lib"
            "${VISIONG_SDK_ROOT}/ive/lib"
        )
        if(_ive_from_sdk)
            set(VISIONG_IVE_LIB_DIR "${_ive_from_sdk}" CACHE PATH "Directory containing prebuilt IVE libraries." FORCE)
        endif()
    endif()

    if(VISIONG_PYTHON_ROOT STREQUAL "")
        visiong_first_existing(_python_root_from_sdk
            "${VISIONG_SDK_ROOT}/3rdparty/target_python"
            "${VISIONG_SDK_ROOT}/target_python"
            "${VISIONG_SDK_ROOT}/python"
        )
        if(_python_root_from_sdk)
            set(VISIONG_PYTHON_ROOT "${_python_root_from_sdk}" CACHE PATH "Directory containing the target Python runtime sysroot." FORCE)
        endif()
    endif()

    if(VISIONG_VENDOR_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_vendor_include_from_sdk
            "${VISIONG_SDK_ROOT}/vendor/rockchip/include"
            "${VISIONG_SDK_ROOT}/include"
        )
        if(_vendor_include_from_sdk)
            set(VISIONG_VENDOR_INCLUDE_DIR "${_vendor_include_from_sdk}" CACHE PATH "Directory containing Rockchip SDK headers (rk_*, rkaiq, sample_comm)." FORCE)
        endif()
    endif()

    if(VISIONG_RGA_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_rga_include_from_sdk
            "${VISIONG_SDK_ROOT}/vendor/librga/include"
            "${VISIONG_SDK_ROOT}/3rdparty/librga/include"
            "${VISIONG_SDK_ROOT}/librga/include"
            "${VISIONG_SDK_ROOT}/include"
        )
        if(_rga_include_from_sdk)
            set(VISIONG_RGA_INCLUDE_DIR "${_rga_include_from_sdk}" CACHE PATH "Directory containing librga headers (im2d.h)." FORCE)
        endif()
    endif()

    if(VISIONG_IVE_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_ive_include_from_sdk
            "${VISIONG_SDK_ROOT}/media/ive/ive/include"
            "${VISIONG_SDK_ROOT}/3rdparty/ive/include"
            "${VISIONG_SDK_ROOT}/ive/include"
            "${VISIONG_SDK_ROOT}/include"
        )
        if(_ive_include_from_sdk)
            set(VISIONG_IVE_INCLUDE_DIR "${_ive_include_from_sdk}" CACHE PATH "Directory containing IVE headers (rk_mpi_ive.h)." FORCE)
        endif()
    endif()

    if(VISIONG_RKNN_INCLUDE_DIR STREQUAL "")
        visiong_first_existing(_rknn_include_from_sdk
            "${VISIONG_SDK_ROOT}/vendor/rknpu2/include"
            "${VISIONG_SDK_ROOT}/3rdparty/rknpu2/include"
            "${VISIONG_SDK_ROOT}/rknpu2/include"
        )
        if(_rknn_include_from_sdk)
            set(VISIONG_RKNN_INCLUDE_DIR "${_rknn_include_from_sdk}" CACHE PATH "Directory containing RKNN runtime headers (rknn_api.h)." FORCE)
        endif()
    endif()
endif()

if(VISIONG_PYTHON_INCLUDE_DIR STREQUAL "")
    visiong_first_existing(_python_include_default
        "${VISIONG_PYTHON_ROOT}/include/python3.11"
    )
    if(_python_include_default)
        set(VISIONG_PYTHON_INCLUDE_DIR "${_python_include_default}" CACHE PATH "Path to the target Python include directory." FORCE)
    endif()
endif()
if(VISIONG_PYTHON_LIBRARY STREQUAL "")
    visiong_first_existing(_python_library_default
        "${VISIONG_PYTHON_ROOT}/lib/libpython3.11.so"
    )
    if(_python_library_default)
        set(VISIONG_PYTHON_LIBRARY "${_python_library_default}" CACHE FILEPATH "Path to the target Python shared library." FORCE)
    endif()
endif()
if(VISIONG_OPENCV_DIR STREQUAL "")
    visiong_first_existing(_opencv_default
        "${VISIONG_OPENCV_ROOT}/lib/cmake/opencv4"
        "${VISIONG_OPENCV_ROOT}/arm-linux-uclibcgnueabihf/lib/cmake/opencv4"
        "${VISIONG_VENDOR_LIB_DIR}/cmake/opencv4"
    )
    if(_opencv_default)
        set(VISIONG_OPENCV_DIR "${_opencv_default}" CACHE PATH "Path to OpenCVConfig.cmake or its containing directory." FORCE)
    endif()
endif()


if(VISIONG_OPENCV_DIR AND NOT IS_DIRECTORY "${VISIONG_OPENCV_DIR}")
    get_filename_component(OpenCV_DIR "${VISIONG_OPENCV_DIR}" DIRECTORY)
else()
    set(OpenCV_DIR "${VISIONG_OPENCV_DIR}")
endif()

visiong_require_path("${VISIONG_VENDOR_LIB_DIR}" "Vendor library directory not found. Use --deps-root, --sdk-root, or set VISIONG_VENDOR_LIB_DIR explicitly.")
if(VISIONG_ENABLE_IVE)
    visiong_require_path("${VISIONG_IVE_LIB_DIR}" "IVE library directory not found. Use --deps-root, --sdk-root, or set VISIONG_IVE_LIB_DIR explicitly.")
endif()
visiong_require_path("${OpenCV_DIR}" "OpenCV config directory not found. Use --deps-root, --sdk-root, or set VISIONG_OPENCV_DIR explicitly.")
visiong_require_path("${VISIONG_VENDOR_INCLUDE_DIR}" "Rockchip SDK header directory not found. Use --deps-root, --sdk-root, or set VISIONG_VENDOR_INCLUDE_DIR explicitly.")
visiong_require_path("${VISIONG_RGA_INCLUDE_DIR}/im2d.h" "librga headers were not found. Use --deps-root, --sdk-root, or set VISIONG_RGA_INCLUDE_DIR explicitly.")
if(VISIONG_ENABLE_IVE)
    visiong_require_path("${VISIONG_IVE_INCLUDE_DIR}/rk_mpi_ive.h" "IVE headers were not found. Use --deps-root, --sdk-root, or set VISIONG_IVE_INCLUDE_DIR explicitly.")
endif()
if(VISIONG_ENABLE_NPU)
    visiong_require_path("${VISIONG_RKNN_INCLUDE_DIR}/rknn_api.h" "rknn_api.h was not found. Use --deps-root, --sdk-root, or set VISIONG_RKNN_INCLUDE_DIR explicitly.")
endif()
if(VISIONG_BUILD_PYTHON_MODULE)
    visiong_require_path("${VISIONG_PYTHON_INCLUDE_DIR}/Python.h" "Python.h was not found. Use --deps-root, --sdk-root, or set VISIONG_PYTHON_INCLUDE_DIR explicitly.")
    visiong_require_path("${VISIONG_PYTHON_LIBRARY}" "The target Python shared library was not found. Use --deps-root, --sdk-root, or set VISIONG_PYTHON_LIBRARY explicitly.")
endif()

message(STATUS "VisionG components: IVE=${VISIONG_ENABLE_IVE}, NPU=${VISIONG_ENABLE_NPU}, GUI=${VISIONG_ENABLE_GUI}")
message(STATUS "VisionG deps root: ${VISIONG_DEPS_ROOT}")
message(STATUS "VisionG vendor lib dir: ${VISIONG_VENDOR_LIB_DIR}")
if(VISIONG_ENABLE_IVE)
    message(STATUS "VisionG IVE lib dir: ${VISIONG_IVE_LIB_DIR}")
endif()
message(STATUS "VisionG target Python include: ${VISIONG_PYTHON_INCLUDE_DIR}")
message(STATUS "VisionG target Python library: ${VISIONG_PYTHON_LIBRARY}")
message(STATUS "VisionG OpenCV root: ${VISIONG_OPENCV_ROOT}")
message(STATUS "VisionG OpenCV config dir: ${OpenCV_DIR}")
message(STATUS "VisionG vendor include dir: ${VISIONG_VENDOR_INCLUDE_DIR}")
message(STATUS "VisionG RGA include dir: ${VISIONG_RGA_INCLUDE_DIR}")
if(VISIONG_ENABLE_IVE)
    message(STATUS "VisionG IVE include dir: ${VISIONG_IVE_INCLUDE_DIR}")
endif()
if(VISIONG_ENABLE_NPU)
    message(STATUS "VisionG RKNN include dir: ${VISIONG_RKNN_INCLUDE_DIR}")
endif()

find_package(OpenCV REQUIRED PATHS "${OpenCV_DIR}" NO_DEFAULT_PATH)

set(VISIONG_OPT_LEVEL "-O3")
if(NOT VISIONG_PERF_FIRST)
    set(VISIONG_OPT_LEVEL "-Os")
endif()
set(VISIONG_FAST_MATH_FLAG "")
if(VISIONG_ENABLE_FAST_MATH)
    set(VISIONG_FAST_MATH_FLAG "-ffast-math")
endif()
set(VISIONG_OPT_FLAGS "-mcpu=cortex-a7 -mfloat-abi=hard -mfpu=neon-vfpv4 ${VISIONG_OPT_LEVEL} ${VISIONG_FAST_MATH_FLAG} -fomit-frame-pointer -DNDEBUG")
set(VISIONG_SIZE_FLAGS "-ffunction-sections -fdata-sections -fno-ident -fvisibility=hidden -fno-strict-aliasing")
set(VISIONG_WARN_FLAGS -Wall -Wextra -Wformat -Wformat-security)

set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${VISIONG_OPT_FLAGS} ${VISIONG_SIZE_FLAGS}")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${VISIONG_OPT_FLAGS} ${VISIONG_SIZE_FLAGS}")
set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl,--gc-sections")
if(VISIONG_STRIP_SYMBOLS)
    set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl,--strip-all")
endif()
if(VISIONG_ENABLE_LTO)
    include(CheckIPOSupported)
    check_ipo_supported(RESULT _visiong_lto_supported OUTPUT _visiong_lto_error)
    if(_visiong_lto_supported)
        set(CMAKE_INTERPROCEDURAL_OPTIMIZATION TRUE)
    else()
        message(WARNING "LTO requested but unavailable: ${_visiong_lto_error}")
    endif()
endif()

# Generate component metadata once so both the build tree and release packages share the same switches. / Generate component 元数据 一次 so both 构建 tree 与 release packages share same switches.
# 生成统一的组件元数据，让构建目录与发布包使用同一组开关。
set(VISIONG_GENERATED_INCLUDE_DIR "${CMAKE_BINARY_DIR}/generated")
file(MAKE_DIRECTORY "${VISIONG_GENERATED_INCLUDE_DIR}/visiong/common")
set(VISIONG_WITH_IVE ${VISIONG_ENABLE_IVE})
set(VISIONG_WITH_NPU ${VISIONG_ENABLE_NPU})
set(VISIONG_WITH_GUI ${VISIONG_ENABLE_GUI})
configure_file(
    "${VISIONG_ROOT}/include/visiong/common/build_config.h.in"
    "${VISIONG_GENERATED_INCLUDE_DIR}/visiong/common/build_config.h"
    @ONLY
)
configure_file("${VISIONG_ROOT}/cmake/VisionGConfig.cmake.in" "${CMAKE_BINARY_DIR}/VisionGConfig.cmake" @ONLY)
configure_file("${VISIONG_ROOT}/cmake/VisionGConfigVersion.cmake.in" "${CMAKE_BINARY_DIR}/VisionGConfigVersion.cmake" @ONLY)
configure_file("${VISIONG_ROOT}/cmake/VisionGTargets.cmake.in" "${CMAKE_BINARY_DIR}/VisionGTargets.cmake" @ONLY)

set(VISIONG_CORE_SRCS
    src/core/BufferStateMachine.cpp
    src/core/Camera.cpp
    src/core/ImageBuffer.cpp
    src/core/ImageBufferAnalysis.cpp
    src/core/ImageBufferDrawing.cpp
    src/common/LegacyKey.cpp
    src/core/ImageBufferMethods.cpp
    src/common/internal/dma_alloc.cpp
    src/common/internal/stb_impl.cpp
    src/core/internal/log_filter.cpp
    src/core/internal/logger.cpp
    src/core/NetUtils.cpp
    src/core/NpuClock.cpp
    src/core/PinMux.cpp
    src/core/PixelFormat.cpp
    src/core/RgaHelper.cpp
)

set(VISIONG_MODULE_BASE_SRCS
    src/modules/DisplayFB.cpp
    src/modules/DisplayHTTP.cpp
    src/modules/DisplayHTTPFLV.cpp
    src/modules/DisplayRTSP.cpp
    src/modules/DisplayUDP.cpp
    src/modules/internal/font_support.cpp
    src/modules/internal/gui_render_utils.cpp
    src/modules/ISPController.cpp
    src/modules/Touch.cpp
    src/modules/VencManager.cpp
    src/modules/VencManagerEncode.cpp
    src/modules/VencRecorder.cpp
)

set(VISIONG_MODULE_GUI_SRCS
    src/modules/internal/gui_nuklear_style.cpp
    src/modules/internal/gui_command_renderer.cpp
    src/modules/internal/gui_manager_input.cpp
    src/modules/internal/gui_manager_widgets.cpp
    src/modules/GUIManager.cpp
    src/modules/GUIManagerApi.cpp
    src/modules/GUIManagerBridge.cpp
    src/modules/GUIManagerCompat.cpp
)

set(VISIONG_MODULE_IVE_SRCS
    src/modules/internal/ive_memory.cpp
    src/modules/IVE.cpp
    src/modules/IVEAdvancedOps.cpp
)

set(VISIONG_C_COMPAT_SRCS
    src/c_compat/base64.c
    src/c_compat/http-header-content-type.c
    src/c_compat/http-parser.c
    src/c_compat/http-reason.c
    src/c_compat/rfc822-datetime.c
)

set(VISIONG_NPU_SRCS
    src/npu/LowLevelNPU.cpp
    src/npu/NPU.cpp
    src/npu/NanoTrack.cpp
    src/npu/PPOCR.cpp
    src/npu/facenet.cpp
    src/npu/lprnet.cpp
    src/npu/retinaface.cpp
    src/npu/yolo11.cpp
    src/npu/yolo11_pose.cpp
    src/npu/yolo11_seg.cpp
    src/npu/yolov5.cpp
    src/npu/internal/tracking/rknn_engine.cpp
    src/npu/internal/tracking/core.cpp
)

set(VISIONG_PYTHON_BINDING_SRCS
    src/python/bind_camera_ive.cpp
    src/python/bind_core_image.cpp
    src/python/bind_io_npu_gui.cpp
    src/python/bind_pinmux.cpp
    src/python/bind_streaming.cpp
    src/python/bindings_common.cpp
    src/python/license_guard.cpp
    src/python/visiong_bindings.cpp
)

set(VISIONG_THIRD_PARTY_C_SRCS
    3rdparty/media-server/libflv/source/mpeg4-avc.c
    3rdparty/media-server/libflv/source/mpeg4-hevc.c
    3rdparty/media-server/libflv/source/mpeg4-annexbtomp4.c
    3rdparty/media-server/libflv/source/hevc-annexbtomp4.c
    3rdparty/media-server/libflv/source/flv-muxer.c
    3rdparty/media-server/libflv/source/flv-header.c
    3rdparty/media-server/libflv/source/flv-writer.c
    3rdparty/media-server/libflv/source/amf0.c
    3rdparty/media-server/libflv/source/aom-av1.c
    3rdparty/media-server/libflv/source/avswg-avs3.c
    3rdparty/media-server/libflv/source/mp3-header.c
    3rdparty/media-server/libflv/source/mpeg4-aac.c
    3rdparty/media-server/libflv/source/mpeg4-aac-asc.c
    3rdparty/media-server/libflv/source/mpeg4-vvc.c
    3rdparty/media-server/libflv/source/opus-head.c
    3rdparty/media-server/libflv/source/vvc-annexbtomp4.c
    3rdparty/media-server/libmov/source/mov-avc1.c
    3rdparty/media-server/libmov/source/mov-dinf.c
    3rdparty/media-server/libmov/source/mov-elst.c
    3rdparty/media-server/libmov/source/mov-esds.c
    3rdparty/media-server/libmov/source/mov-ftyp.c
    3rdparty/media-server/libmov/source/mov-hdlr.c
    3rdparty/media-server/libmov/source/mov-hdr.c
    3rdparty/media-server/libmov/source/mov-iods.c
    3rdparty/media-server/libmov/source/mov-leva.c
    3rdparty/media-server/libmov/source/mov-mdhd.c
    3rdparty/media-server/libmov/source/mov-mehd.c
    3rdparty/media-server/libmov/source/mov-mfhd.c
    3rdparty/media-server/libmov/source/mov-minf.c
    3rdparty/media-server/libmov/source/mov-mvhd.c
    3rdparty/media-server/libmov/source/mov-opus.c
    3rdparty/media-server/libmov/source/mov-reader.c
    3rdparty/media-server/libmov/source/mov-sidx.c
    3rdparty/media-server/libmov/source/mov-stco.c
    3rdparty/media-server/libmov/source/mov-stsc.c
    3rdparty/media-server/libmov/source/mov-stsd.c
    3rdparty/media-server/libmov/source/mov-stss.c
    3rdparty/media-server/libmov/source/mov-stsz.c
    3rdparty/media-server/libmov/source/mov-stts.c
    3rdparty/media-server/libmov/source/mov-tag.c
    3rdparty/media-server/libmov/source/mov-tfdt.c
    3rdparty/media-server/libmov/source/mov-tfhd.c
    3rdparty/media-server/libmov/source/mov-tfra.c
    3rdparty/media-server/libmov/source/mov-tkhd.c
    3rdparty/media-server/libmov/source/mov-track.c
    3rdparty/media-server/libmov/source/mov-trex.c
    3rdparty/media-server/libmov/source/mov-trun.c
    3rdparty/media-server/libmov/source/mov-tx3g.c
    3rdparty/media-server/libmov/source/mov-udta.c
    3rdparty/media-server/libmov/source/mov-vpcc.c
    3rdparty/media-server/libmov/source/mov-writer.c
    3rdparty/media-server/libmov/source/fmp4-reader.c
    3rdparty/media-server/libmov/source/fmp4-writer.c
    3rdparty/media-server/librtsp/source/rtsp-reason.c
    3rdparty/media-server/librtsp/source/rtsp-header-range.c
    3rdparty/media-server/librtsp/source/rtsp-header-rtp-info.c
    3rdparty/media-server/librtsp/source/rtsp-header-session.c
    3rdparty/media-server/librtsp/source/rtsp-header-transport.c
    3rdparty/media-server/librtsp/source/server/rtsp-server.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-handler.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-options.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-describe.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-setup.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-play.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-pause.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-teardown.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-parameter.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-announce.c
    3rdparty/media-server/librtsp/source/server/rtsp-server-record.c
    3rdparty/media-server/librtp/source/rtp-packet.c
    3rdparty/media-server/librtp/payload/rtp-h264-bitstream.c
    3rdparty/media-server/librtp/payload/rtp-h264-pack.c
    3rdparty/media-server/librtp/payload/rtp-h265-pack.c
)

set(VISIONG_SELECTED_MODULE_SRCS
    ${VISIONG_MODULE_BASE_SRCS}
)
if(VISIONG_ENABLE_GUI)
    list(APPEND VISIONG_SELECTED_MODULE_SRCS ${VISIONG_MODULE_GUI_SRCS})
endif()
if(VISIONG_ENABLE_IVE)
    list(APPEND VISIONG_SELECTED_MODULE_SRCS ${VISIONG_MODULE_IVE_SRCS})
endif()

set(VISIONG_SELECTED_NPU_SRCS "")
if(VISIONG_ENABLE_NPU)
    set(VISIONG_SELECTED_NPU_SRCS ${VISIONG_NPU_SRCS})
endif()

foreach(_src IN LISTS VISIONG_CORE_SRCS VISIONG_SELECTED_MODULE_SRCS VISIONG_C_COMPAT_SRCS VISIONG_SELECTED_NPU_SRCS VISIONG_PYTHON_BINDING_SRCS VISIONG_THIRD_PARTY_C_SRCS)
    if(NOT EXISTS "${VISIONG_ROOT}/${_src}")
        message(FATAL_ERROR "Configured build input is missing: ${_src}")
    endif()
endforeach()

add_subdirectory(3rdparty/quirc)

set(VISIONG_RUNTIME_SRCS
    ${VISIONG_CORE_SRCS}
    ${VISIONG_SELECTED_MODULE_SRCS}
    ${VISIONG_C_COMPAT_SRCS}
    ${VISIONG_SELECTED_NPU_SRCS}
    ${VISIONG_THIRD_PARTY_C_SRCS}
)

set(VISIONG_COMMON_COMPILE_DEFINITIONS
    VERSION_INFO="${VISIONG_VERSION}"
    RV1106_1103
)
if(VISIONG_ENABLE_IVE)
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_IVE=1)
else()
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_IVE=0)
endif()
if(VISIONG_ENABLE_NPU)
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_NPU=1)
else()
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_NPU=0)
endif()
if(VISIONG_ENABLE_GUI)
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_GUI=1)
else()
    list(APPEND VISIONG_COMMON_COMPILE_DEFINITIONS VISIONG_WITH_GUI=0)
endif()

set(VISIONG_PUBLIC_INCLUDE_DIRS
    include
    ${VISIONG_GENERATED_INCLUDE_DIR}
    ${VISIONG_VENDOR_INCLUDE_DIR}
    ${VISIONG_VENDOR_INCLUDE_DIR}/rockchip
    ${VISIONG_RGA_INCLUDE_DIR}
    ${OpenCV_INCLUDE_DIRS}
)
if(VISIONG_ENABLE_IVE)
    list(APPEND VISIONG_PUBLIC_INCLUDE_DIRS ${VISIONG_IVE_INCLUDE_DIR})
endif()
if(VISIONG_ENABLE_NPU)
    list(APPEND VISIONG_PUBLIC_INCLUDE_DIRS ${VISIONG_RKNN_INCLUDE_DIR})
endif()

set(VISIONG_COMMON_INCLUDE_DIRS
    ${VISIONG_PUBLIC_INCLUDE_DIRS}
    src/c_compat/include
    src
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/common
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/xcore
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/xcore/base
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/uAPI2
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/algos
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/iq_parser
    ${VISIONG_VENDOR_INCLUDE_DIR}/rkaiq/iq_parser_v2
    3rdparty/pybind11
    3rdparty/quirc
    3rdparty/stb
    3rdparty/nuklear
    3rdparty/media-server/libmov/include
    3rdparty/media-server/libflv/include
    3rdparty/media-server/librtsp/include
    3rdparty/media-server/librtp/include
    3rdparty/media-server/librtp/payload
)

visiong_first_existing(VISIONG_LIB_RGA
    "${VISIONG_VENDOR_LIB_DIR}/librga.so"
    "${VISIONG_VENDOR_LIB_DIR}/librga.a"
)
visiong_first_existing(VISIONG_LIB_MPP
    "${VISIONG_VENDOR_LIB_DIR}/librockchip_mpp.so.1"
    "${VISIONG_VENDOR_LIB_DIR}/librockchip_mpp.so.0"
    "${VISIONG_VENDOR_LIB_DIR}/librockchip_mpp.a"
)
visiong_first_existing(VISIONG_LIB_RKAIQ
    "${VISIONG_VENDOR_LIB_DIR}/librkaiq.so"
    "${VISIONG_VENDOR_LIB_DIR}/librkaiq.a"
)
visiong_first_existing(VISIONG_LIB_ROCKIT
    "${VISIONG_VENDOR_LIB_DIR}/librockit.so"
    "${VISIONG_VENDOR_LIB_DIR}/librockit.a"
)
visiong_first_existing(VISIONG_LIB_RTSP
    "${VISIONG_VENDOR_LIB_DIR}/librtsp.a"
)
set(VISIONG_LIB_ROCKIVA "")
set(VISIONG_LIB_RKNNMRT "")
if(VISIONG_ENABLE_NPU)
    visiong_first_existing(VISIONG_LIB_ROCKIVA
        "${VISIONG_VENDOR_LIB_DIR}/librockiva.so"
        "${VISIONG_VENDOR_LIB_DIR}/librockiva.a"
    )
    visiong_first_existing(VISIONG_LIB_RKNNMRT
        "${VISIONG_VENDOR_LIB_DIR}/librknnmrt.so"
        "${VISIONG_VENDOR_LIB_DIR}/librknnmrt.a"
    )
endif()
set(VISIONG_LIB_RVE "")
set(VISIONG_LIB_IVS "")
if(VISIONG_ENABLE_IVE)
    visiong_first_existing(VISIONG_LIB_RVE
        "${VISIONG_IVE_LIB_DIR}/librve.so"
        "${VISIONG_IVE_LIB_DIR}/librve.a"
    )
    visiong_first_existing(VISIONG_LIB_IVS
        "${VISIONG_IVE_LIB_DIR}/libivs.so"
        "${VISIONG_IVE_LIB_DIR}/libivs.a"
    )
endif()

set(VISIONG_REQUIRED_LIB_VARS
    VISIONG_LIB_RGA
    VISIONG_LIB_MPP
    VISIONG_LIB_RKAIQ
    VISIONG_LIB_ROCKIT
    VISIONG_LIB_RTSP
)
if(VISIONG_ENABLE_NPU)
    list(APPEND VISIONG_REQUIRED_LIB_VARS VISIONG_LIB_ROCKIVA VISIONG_LIB_RKNNMRT)
endif()
if(VISIONG_ENABLE_IVE)
    list(APPEND VISIONG_REQUIRED_LIB_VARS VISIONG_LIB_RVE VISIONG_LIB_IVS)
endif()
foreach(_required_lib IN LISTS VISIONG_REQUIRED_LIB_VARS)
    if("${${_required_lib}}" STREQUAL "")
        message(FATAL_ERROR "Required vendor library ${_required_lib} was not found. Check VISIONG_SDK_ROOT / VISIONG_VENDOR_LIB_DIR / VISIONG_IVE_LIB_DIR.")
    endif()
endforeach()

set(VISIONG_RUNTIME_LINK_LIBS
    ${OpenCV_LIBS}
    quirc
    ${VISIONG_LIB_RGA}
    ${VISIONG_LIB_MPP}
    ${VISIONG_LIB_RKAIQ}
    ${VISIONG_LIB_ROCKIT}
    ${VISIONG_LIB_RTSP}
    pthread
    dl
    m
    stdc++fs
)
if(VISIONG_ENABLE_NPU)
    list(APPEND VISIONG_RUNTIME_LINK_LIBS ${VISIONG_LIB_ROCKIVA} ${VISIONG_LIB_RKNNMRT})
endif()
if(VISIONG_ENABLE_IVE)
    list(APPEND VISIONG_RUNTIME_LINK_LIBS ${VISIONG_LIB_RVE} ${VISIONG_LIB_IVS})
endif()

set_source_files_properties(${VISIONG_THIRD_PARTY_C_SRCS} PROPERTIES COMPILE_OPTIONS "-w")
set_source_files_properties(${VISIONG_PYTHON_BINDING_SRCS} PROPERTIES COMPILE_OPTIONS "-Wno-missing-field-initializers")

add_library(visiong_runtime_objects OBJECT ${VISIONG_RUNTIME_SRCS})
visiong_apply_common_compile_settings(visiong_runtime_objects)
target_compile_options(visiong_runtime_objects PRIVATE -fvisibility=default)
target_link_libraries(visiong_runtime_objects PUBLIC quirc)
target_include_directories(visiong_runtime_objects
    PUBLIC
        ${VISIONG_PUBLIC_INCLUDE_DIRS}
    PRIVATE
        ${VISIONG_COMMON_INCLUDE_DIRS}
)

if(VISIONG_BUILD_CPP_LIBS)
    add_library(visiong_runtime_shared SHARED $<TARGET_OBJECTS:visiong_runtime_objects>)
    target_link_libraries(visiong_runtime_shared PUBLIC ${VISIONG_RUNTIME_LINK_LIBS})
    target_link_options(visiong_runtime_shared PRIVATE "-Wl,-z,global")
    target_include_directories(visiong_runtime_shared
        PUBLIC
            ${VISIONG_PUBLIC_INCLUDE_DIRS}
        PRIVATE
            ${VISIONG_COMMON_INCLUDE_DIRS}
    )
    set_target_properties(visiong_runtime_shared PROPERTIES
        OUTPUT_NAME "visiong"
        BUILD_RPATH "$ORIGIN:/oem/usr/lib"
        INSTALL_RPATH "$ORIGIN:/oem/usr/lib"
    )

    add_library(visiong_runtime_static STATIC $<TARGET_OBJECTS:visiong_runtime_objects>)
    target_link_libraries(visiong_runtime_static PUBLIC ${VISIONG_RUNTIME_LINK_LIBS})
    target_include_directories(visiong_runtime_static
        PUBLIC
            ${VISIONG_PUBLIC_INCLUDE_DIRS}
        PRIVATE
            ${VISIONG_COMMON_INCLUDE_DIRS}
    )
    set_target_properties(visiong_runtime_static PROPERTIES OUTPUT_NAME "visiong")
else()
    message(STATUS "VISIONG_BUILD_CPP_LIBS=OFF: skipping libvisiong shared/static targets.")
endif()

if(VISIONG_BUILD_PYTHON_MODULE)
    set(VISIONG_PYTHON_MODULE_SRCS
        ${VISIONG_PYTHON_BINDING_SRCS}
        $<TARGET_OBJECTS:visiong_runtime_objects>
    )
    add_library(visiong_python_module SHARED ${VISIONG_PYTHON_MODULE_SRCS})
    visiong_apply_common_compile_settings(visiong_python_module)
    target_include_directories(visiong_python_module PRIVATE ${VISIONG_COMMON_INCLUDE_DIRS})
    target_include_directories(visiong_python_module SYSTEM PRIVATE ${VISIONG_PYTHON_INCLUDE_DIR})
    target_compile_options(visiong_python_module PRIVATE -fvisibility=default)
    target_link_libraries(visiong_python_module PRIVATE ${VISIONG_RUNTIME_LINK_LIBS} ${VISIONG_PYTHON_LIBRARY})
    target_link_options(visiong_python_module PRIVATE "-Wl,-z,global")
    set_target_properties(visiong_python_module PROPERTIES
        PREFIX ""
        OUTPUT_NAME "_visiong"
        SUFFIX ".so"
        BUILD_RPATH "$ORIGIN:/oem/usr/lib"
        INSTALL_RPATH "$ORIGIN:/oem/usr/lib"
    )
else()
    message(STATUS "VISIONG_BUILD_PYTHON_MODULE=OFF: skipping _visiong.so build.")
endif()

set(CMAKE_INSTALL_RPATH "$ORIGIN:/oem/usr/lib")
set(CMAKE_BUILD_WITH_INSTALL_RPATH ON)

if(NOT VISIONG_BUILD_PYTHON_MODULE AND NOT VISIONG_BUILD_CPP_LIBS)
    message(WARNING "Both VISIONG_BUILD_PYTHON_MODULE and VISIONG_BUILD_CPP_LIBS are OFF; no build targets are configured.")
endif()
